{{- if .Values.validatingAdmissionPolicies.applyPolicies -}}
{{ include "secrets-store-sync-controller.admissionApiVersion" . }}
kind: ValidatingAdmissionPolicy
metadata:
  name: "secrets-store-sync-controller-create-update-policy"
  annotations:
    kubernetes.io/description: |
      The policy checks the secrets-store-sync-controller updates Secrets so that:
      - there is only a single owner reference matching the SecretSync of the same name
      - the secret is not a service account token secret
      - the type of the secret matches any type in a pre-defined allowlist
spec:
  failurePolicy: Fail
  matchConditions:
    - name: 'user-is-secrets-store-sync-controller'
      expression: {{ include "chartname.matchConditionExpression" . | quote }}
  matchConstraints:
    resourceRules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["secrets"]
  variables:
    - name: hasOneSecretSyncOwner
      expression: "(has(object.metadata.ownerReferences) && size(object.metadata.ownerReferences) == 1 && object.metadata.ownerReferences.all(o, o.kind == 'SecretSync' && o.apiVersion.startsWith('secret-sync.x-k8s.io/') && o.name == object.metadata.name))"
    - name: deniedSecretTypes
      expression: object.type == "kubernetes.io/service-account-token"
    - name: allowedSecretTypes
      expression: {{ include "chartname.secretTypesList" .Values.validatingAdmissionPolicies.allowedSecretTypes | quote }}
  validations:
    - expression: "variables.deniedSecretTypes == false"
      message: "Secrets with type \"kubernetes.io/service-account-token\" are not allowed."
      messageExpression: "'secrets-store-sync-controller has failed to ' +  string(request.operation) + ' secret with ' + string(object.type) + ' type ' + 'in the ' + string(request.namespace) + ' namespace. The controller is not allowed to create or update secrets with this type.'"
    - expression: "variables.allowedSecretTypes == true && variables.hasOneSecretSyncOwner == true"
      message: "Only secrets with types defined in the allowedSecretTypes are allowed."
      messageExpression: "'secrets-store-sync-controller has failed to ' +  string(request.operation) + ' secret with ' + string(object.type) + ' type ' + 'in the ' + string(request.namespace) + ' namespace. The controller can only create or update secrets in the allowed types list with a single secretsync owner.'"
{{- end -}}

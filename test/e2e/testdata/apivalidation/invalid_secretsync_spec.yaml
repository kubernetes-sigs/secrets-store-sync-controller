apiVersion: secret-sync.x-k8s.io/v1alpha1
kind: SecretSync
metadata:
  name: test-secret-sync
  annotations:
    e2e.test-name: "invalid secretsync spec"
    e2e.test-expected-errors: "SecretSync.secret-sync.x-k8s.io \"test-secret-sync\" is invalid: spec.serviceAccountName: Invalid value: \"defa:ult\": spec.serviceAccountName in body should match '^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$'"
spec:
  serviceAccountName: defa:ult
  secretProviderClassName: e2e-providerspc
  secretObject:
    type: "ValidType"
    data:
      - sourcePath: foo
        targetKey: bar
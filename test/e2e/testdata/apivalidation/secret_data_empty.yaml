apiVersion: secret-sync.x-k8s.io/v1alpha1
kind: SecretSync
metadata:
  name: test-secret-sync
  annotations:
    e2e.test-name: "secret data is empty"
    e2e.test-expected-errors: "spec.secretObject.data in body should have at least 1 items"
spec:
  serviceAccountName: default
  secretProviderClassName: e2e-providerspc
  secretObject:
    type: "custom.type"
    data: []
apiVersion: secret-sync.x-k8s.io/v1alpha1
kind: SecretSync
metadata:
  name: test-secret-sync
  annotations:
    e2e.test-name: "invalid values in secret data"
    e2e.test-expected-errors: |
      pec.secretProviderClassName: Invalid value: "oh noes!<-invalid": spec.secretProviderClassName in body should match '^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
      spec.serviceAccountName: Invalid value: "thisIsAlsoWrong": spec.serviceAccountName in body should match '^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*
      spec.forceSynchronization in body should match '^[A-Za-z0-9]([-A-Za-z0-9]+([-._a-zA-Z0-9]?[A-Za-z0-9])*)?$'
spec:
  secretSyncControllerName: "not much going on in here!"
  secretProviderClassName: "oh noes!<-invalid"
  serviceAccountName: "thisIsAlsoWrong"
  forceSynchronization: "when I was originally checking this</div><script>console.log('the pattern was unterminated')</script><div>"
  secretObject:
    type: "custom.type"
    data:
      - sourcePath: foo
        targetKey: bar